services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:git 
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Garante o hot reload em alguns sistemas
    stdin_open: true
    tty: true